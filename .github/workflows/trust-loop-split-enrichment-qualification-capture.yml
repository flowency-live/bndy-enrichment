name: Capture split Trust Loop enrichment qualification cohort

on:
  push:
    branches: [main]

permissions:
  contents: write
  id-token: write

concurrency:
  group: trust-loop-split-enrichment-qualification-capture
  cancel-in-progress: false

jobs:
  capture:
    if: contains(github.event.head_commit.message, 'Capture Backline split enrichment qualification cohort')
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: eu-west-2
      - name: Enforce approved one-shot scope
        run: |
          set -euo pipefail
          test ! -e ops/enrichment/google-pse-gemini-structured-unreviewed.json
          test ! -e ops/enrichment/google-pse-gemini-structured-review.md
      - name: Load deployed Gemini credential
        run: |
          set -euo pipefail
          SECRET_ARN=$(aws cloudformation describe-stacks \
            --stack-name BndyEnrichmentStack \
            --query "Stacks[0].Outputs[?OutputKey=='GeminiSecretArn'].OutputValue | [0]" \
            --output text)
          test -n "$SECRET_ARN"
          test "$SECRET_ARN" != "None"
          GEMINI_API_KEY=$(aws secretsmanager get-secret-value \
            --secret-id "$SECRET_ARN" \
            --query SecretString \
            --output text | jq -r '.apiKey // empty')
          test -n "$GEMINI_API_KEY"
          echo "::add-mask::$GEMINI_API_KEY"
          echo "GEMINI_API_KEY=$GEMINI_API_KEY" >> "$GITHUB_ENV"
      - name: Capture approved bounded split-provider cases
        env:
          GOOGLE_SEARCH_API_KEY: ${{ secrets.GOOGLE_SEARCH_API_KEY }}
          GOOGLE_SEARCH_ENGINE_ID: ${{ vars.GOOGLE_SEARCH_ENGINE_ID }}
          GEMINI_MODEL: gemini-3.6-flash
        run: |
          set -euo pipefail
          test -n "$GOOGLE_SEARCH_API_KEY"
          test -n "$GOOGLE_SEARCH_ENGINE_ID"
          test -n "$GEMINI_API_KEY"
          echo "::add-mask::$GOOGLE_SEARCH_API_KEY"
          npm run trust-loop:capture-split-enrichment | tee trust-loop-split-enrichment-capture.log
          jq -e '
            .providerId == "google-pse-gemini-structured-v1"
            and .cases == 20
            and .artistCases == 10
            and .venueCases == 10
            and .canonicalWrites == 0
            and .reviewStatus == "unreviewed"
            and .totalEstimatedCost <= 0.60
          ' ops/enrichment/google-pse-gemini-structured-unreviewed.json >/dev/null
      - name: Record the unreviewed public evidence cohort
        run: |
          set -euo pipefail
          git config user.name bndy-backline-bot
          git config user.email actions@users.noreply.github.com
          git pull --rebase --autostash origin main
          git add \
            ops/enrichment/google-pse-gemini-structured-unreviewed.json \
            ops/enrichment/google-pse-gemini-structured-review.md
          git commit -m "Record unreviewed Backline split enrichment evidence cohort"
          git push origin HEAD:main
      - name: Resolve the deployed StateTable
        id: resources
        run: |
          set -euo pipefail
          STATE_TABLE=$(aws cloudformation describe-stacks \
            --stack-name BndyEnrichmentStack \
            --query "Stacks[0].Outputs[?OutputKey=='StateTableName'].OutputValue | [0]" \
            --output text)
          test -n "$STATE_TABLE"
          test "$STATE_TABLE" != "None"
          echo "state_table=$STATE_TABLE" >> "$GITHUB_OUTPUT"
      - name: Publish the read-only qualification summary
        env:
          STATE_TABLE: ${{ steps.resources.outputs.state_table }}
          QUALIFICATION_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          QUALIFICATION_ARTIFACT_URL: https://github.com/${{ github.repository }}/blob/main/ops/enrichment/google-pse-gemini-structured-unreviewed.json
          QUALIFICATION_REVIEW_URL: https://github.com/${{ github.repository }}/blob/main/ops/enrichment/google-pse-gemini-structured-review.md
        run: |
          set -euo pipefail
          npm run enrichment:qualification:publish -- \
            ops/enrichment/google-pse-gemini-structured-unreviewed.json \
            | tee qualification-publish.log
          tail -n 1 qualification-publish.log | jq -e '
            .status == "published"
            and .providerId == "google-pse-gemini-structured-v1"
            and .canonicalWrites == 0
            and .cases == 20
            and .gateStatus == (if .captureErrors > 0 then "capture-failed" else "awaiting-human-review" end)
          ' >/dev/null
      - name: Require complete measured capture for qualification review
        run: |
          set -euo pipefail
          jq -e '
            .captureErrors == 0
            and .measuredUsageCases == 20
            and .costMeasurement == "complete"
            and .canonicalWrites == 0
            and .totalEstimatedCost <= 0.60
          ' ops/enrichment/google-pse-gemini-structured-unreviewed.json >/dev/null
